# Git

```bash
git config user.name "DamianS-eng"
```

```bash
# Use --global for all, omit for only one repository
git config --global user.name "DamianS-eng"
git config --global user.email "damiansuaz@gmail.com"
git config --global core.editor "vim"
```


